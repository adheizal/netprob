# Vite development server
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173

# NetProb backend used by the Vite /api and /ws proxies
VITE_API_TARGET=http://127.0.0.1:8080

# Optional `npm run preview` port
VITE_PREVIEW_PORT=4173
